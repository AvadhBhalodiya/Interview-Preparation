# Secret token that unlocks owner-only "mark as read" sync across your devices.
# - Production: set this in Netlify → Site settings → Environment variables.
# - Local: copy this file to `.env` (git-ignored) and run `netlify dev`.
# Treat it like a password — anyone with it can edit your read progress.
OWNER_TOKEN=
